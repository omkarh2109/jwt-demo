export interface IEnvironment {
  production: boolean;
  envName: string;
  apiUrl: string;
  reportUrl: string;
  instagram_token: string;
  stripe_token: string;
  paypal_token: string;
  redirectUrl: string;
}